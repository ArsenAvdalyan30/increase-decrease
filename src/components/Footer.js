import React, { Component } from "react";

class Footer extends Component {
  render() {
    return (
      <div className="footer">
        Footer<button>click footer</button>
      </div>
    );
  }
}

export default Footer;
