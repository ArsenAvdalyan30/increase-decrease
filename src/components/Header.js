import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <div className="header">
        Header<button>click header</button>
      </div>
    );
  }
}

export default Header;
